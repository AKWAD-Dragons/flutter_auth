name: auth_provider_example
description: Demonstrates how to use the auth_provider plugin.

publish_to: 'none'

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: 4.0.4
  fly_networking: ^2.0.0

  auth_provider:
    path: ../auth_provider

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
